curl -H "Authorization: token $GITHUB_TOKEN" -X POST --data-urlencode "{\"body\": \"$(cat results.txt | sed "s/\"/'/g")\"}" "https://api.github.com/repos/${TRAVIS_REPO_SLUG}/issues/${TRAVIS_PULL_REQUEST}/comments"